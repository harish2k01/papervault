# PaperVault API

FastAPI application package for PaperVault.

The backend is organized by feature packages with thin HTTP routes and application services for business workflows. Current capabilities include health checks, uploads, embedded-text extraction, local OCR provider integration, document processing, AI outputs, database-backed search, OpenSearch indexing, tags, notifications, local JWT authentication, and admin user management.
